import { RoundBoard } from "@/components/round-board";

/** Round 1: Litoff Mission (Energy Lab / Material Lab / Instrumentation Lab). */
export default function LitoffMissionPage() {
  return <RoundBoard roundSlug="litoff-mission" backHref="/admin" backLabel="Back to Dashboard" />;
}