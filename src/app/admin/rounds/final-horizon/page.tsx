import { RoundBoard } from "@/components/round-board";

/** Round 2: Final Horizon (Miller / Nebula / Singularity). */
export default function FinalHorizonPage() {
  return <RoundBoard roundSlug="final-horizon" backHref="/admin" backLabel="Back to Dashboard" />;
}
